<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Reset Failed</title>
    <style>
        /* Add your CSS styles here */
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            color: #333;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .container {
            max-width: 400px;
            padding: 20px;
            border-radius: 10px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        h2 {
            font-size: 1.5em;
            margin-bottom: 20px;
            color: #e74c3c; /* Red color for emphasis */
        }
        p {
            font-size: 1.2em;
            margin-bottom: 30px;
        }
        a {
            text-decoration: none;
            color: #3498db;
        }
        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Password Reset Failed!</h2>

        <% if (typeof message !== 'undefined') { %>
            <p><%= message %></p>
        <% } else { %>
            <p>Sorry, we were unable to reset your password.</p>
            <p>Please make sure you provided the correct email address and try again later.</p>
            <p>Click <a href="/">here</a> to return to the homepage.</p>
        <% } %>
    </div>
</body>
</html>
